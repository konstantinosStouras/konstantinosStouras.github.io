# Core requirements for the GloVe path (build + score) and the shared core.
#   pip install -r requirements.txt
nltk>=3.8
numpy>=1.24
openpyxl>=3.1   # only needed to read/write .xlsx ideas files; CSV works without it
