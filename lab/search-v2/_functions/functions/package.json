{
  "name": "search-v2-functions",
  "description": "Score-bearing actions for 'Search With and Without Generative AI' (design brief §17.2): queryAI, reveal and nominate as callable Functions, so the prize mapping never reaches the browser.",
  "version": "1.0.0",
  "private": true,
  "engines": { "node": "22" },
  "main": "index.js",
  "scripts": {
    "lint": "node --check index.js"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  }
}
